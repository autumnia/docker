### Proxy Layer 구성
```
    연결 폴더 구성
        mkdir -p  /data/proxysql/data /data/proxysql/conf
        chmod -R 777 /data/proxysql*

    proxysql.cnf 생성 및 644 권한을 준다.
        proxysql.cnf  참조

    
```
