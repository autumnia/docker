# 도커에 Postgres 설치

```
    docker run -it \
    --name postgres -h postgres01 \
    -p 5432:5432 \
    -e POSTGRES_USER=postgres \
    -e POSTGRES_PASSWORD=4y7sV96vA9wv46VR \
    -e PGDATA=/var/lib/postgresql/data/pgdata
    -v /tmp:/var/lib/postgresql/data
    -d postgres:14.1-alpine
  
```



